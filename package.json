{
  "name": "cert-study",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source study hub — any subject, any exam",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-questions.js",
    "import": "node scripts/import-content.js",
    "validate": "node scripts/validate-exam.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
