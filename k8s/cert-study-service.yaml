apiVersion: v1
kind: Service
metadata:
  name: cert-study
  labels:
    app: cert-study
spec:
  selector:
    app: cert-study
  ports:
    - port: 80
      targetPort: 80
  # Use LoadBalancer for cloud providers, or NodePort for local clusters
  type: ClusterIP
